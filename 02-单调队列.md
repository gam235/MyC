# 单调队列

滑动窗口中需要 O(1) 获取最大值/最小值时使用。

```cpp
deque<int> q;
for (int i = 0; i < n; ++i) {
    while (!q.empty() && q.front() <= i - k) q.pop_front();
    while (!q.empty() && a[q.back()] <= a[i]) q.pop_back();
    q.push_back(i);
    if (i >= k - 1) ans.push_back(a[q.front()]);
}
```
