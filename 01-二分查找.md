# 二分查找

## 什么时候想到
区间有单调性，或需要在有序数据中寻找边界。

## 模板：第一个 >= target
```cpp
int lower_bound_index(const vector<int>& a, int target) {
    int l = 0, r = (int)a.size();
    while (l < r) {
        int mid = l + (r - l) / 2;
        if (a[mid] >= target) r = mid;
        else l = mid + 1;
    }
    return l;
}
```

时间 O(log n)，空间 O(1)。统一使用左闭右开区间 `[l,r)`。
