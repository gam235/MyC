# 差分数组

适合大量区间整体加减、最后统一求结果。

```cpp
vector<long long> diff(n + 1);

auto add = [&](int l, int r, long long x) {
    diff[l] += x;
    if (r + 1 < (int)diff.size()) diff[r + 1] -= x;
};

for (int i = 0; i < n; ++i) {
    if (i) diff[i] += diff[i - 1];
    a[i] += diff[i];
}
```
