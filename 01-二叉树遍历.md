# 二叉树遍历

## DFS
```cpp
void dfs(TreeNode* root) {
    if (!root) return;

    // 前序位置
    dfs(root->left);
    // 中序位置
    dfs(root->right);
    // 后序位置
}
```

前序：当前→左→右；中序：左→当前→右；后序：左→右→当前。

## BFS
```cpp
queue<TreeNode*> q;
if (root) q.push(root);

while (!q.empty()) {
    auto u = q.front(); q.pop();
    if (u->left) q.push(u->left);
    if (u->right) q.push(u->right);
}
```
