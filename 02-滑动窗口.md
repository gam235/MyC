# 滑动窗口

## 什么时候想到
连续子数组/子串，并且窗口存在可维护的合法条件。

```cpp
int l = 0;
for (int r = 0; r < n; ++r) {
    // 加入 a[r]

    while (/* 窗口不合法 */) {
        // 移除 a[l]
        ++l;
    }

    // [l,r] 合法
}
```

右端扩张，条件失效时左端收缩。
