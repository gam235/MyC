# 0/1 背包

每件物品最多选择一次。

```cpp
vector<long long> dp(W + 1, 0);

for (int i = 0; i < n; ++i) {
    for (int j = W; j >= w[i]; --j) {
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
    }
}
```

容量必须倒序，避免同一件物品在一轮中被重复使用。复杂度 O(nW)。
