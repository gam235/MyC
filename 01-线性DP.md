# 线性 DP

状态沿一个线性方向转移，例如爬楼梯、打家劫舍等。

核心问题：**dp[i] 表示什么？**

```cpp
vector<long long> dp(n + 1);
dp[0] = /* 初始状态 */;

for (int i = 1; i <= n; ++i) {
    dp[i] = /* 从之前状态转移 */;
}
```

然后确定：转移来源、初始状态、遍历顺序、最终答案位置。
