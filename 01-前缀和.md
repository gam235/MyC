# 前缀和

用于频繁查询连续区间和。

```cpp
vector<long long> pre(n + 1);
for (int i = 1; i <= n; ++i)
    pre[i] = pre[i - 1] + a[i - 1];

long long sum = pre[r + 1] - pre[l]; // 0-based [l,r]
```

预处理 O(n)，单次查询 O(1)。
